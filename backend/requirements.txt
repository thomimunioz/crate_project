# CRATE backend — FastAPI (proxy CORS + DSP opt-in)
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# Capa 2: análisis de audio (opt-in)
yt-dlp>=2024.8.6
librosa>=0.10.2
numpy>=1.26
soundfile>=0.12
# Requiere ffmpeg instalado en el sistema (para recortar el fragmento y decodificar audio).
